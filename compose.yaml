services:

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"                # ONLY container exposed to the host
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - front-tier
      - back-tier
    depends_on:
      frontend:
        condition: service_started
      backend:
        condition: service_healthy   # backend's own HEALTHCHECK (healthcheck.js)
    read_only: true             # container fs is read-only
    tmpfs:
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true

  backend:
    build:
      args:
        - NODE_ENV=development
      context: backend
      target: development
    command: npm run start-watch
    environment:
      - DATABASE_DB=example
      - DATABASE_USER=root
      - DATABASE_PASSWORD=/run/secrets/db-password
      - DATABASE_HOST=db
      - NODE_ENV=development
    expose:
      - "80"                    # reachable inside the network only
    secrets:
      - db-password
    volumes:
      - ./backend/src:/code/src:ro
      - ./backend/package.json:/code/package.json
      - ./backend/package-lock.json:/code/package-lock.json
      - back-notused:/opt/app/node_modules
    networks:
      - back-tier                # nginx <-> backend
      - data-tier                # backend <-> db
    depends_on:
      - db

  db:
    # We use a mariadb image which supports both amd64 & arm64 architecture
    image: mariadb:10.6
    # If you really want to use MySQL, uncomment the following line
    #image: mysql:8.0.27
    command: '--default-authentication-plugin=mysql_native_password'
    restart: always
    secrets:
      - db-password
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - data-tier                # isolated: unreachable from nginx & frontend
    environment:
      - MYSQL_DATABASE=example
      - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/db-password
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  frontend:
    build:
      context: frontend
      target: development
    expose:
      - "3000"                   # reachable inside the network only
    volumes:
      - ./frontend/src:/code/src
      - /code/node_modules
    networks:
      - front-tier                # nginx <-> frontend only

networks:
  front-tier:      # nginx <-> frontend
  back-tier:       # nginx <-> backend
  data-tier:       # backend <-> db (fully isolated from the rest)

volumes:
  back-notused:
  db-data:

secrets:
  db-password:
    file: db/password.txt
